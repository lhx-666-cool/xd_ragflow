命令清单

在有网机器上构建你修改后的镜像
```bash
cd /home/liu/xd_ragflow

docker build -f Dockerfile.deps -t infiniflow/ragflow_deps:latest .
docker build --platform linux/amd64 -f Dockerfile -t ragflow-local:20260329 .
```
在有网机器上导出离线镜像包
```bash
cd /home/liu/xd_ragflow
bash docker/offline_package_images.sh /tmp/ragflow-offline-images-20260329.tar
```
在有网机器上打一个代码包
```bash
cd /home/liu/xd_ragflow
tar --exclude='.git' --exclude='.venv' --exclude='logs' --exclude='docker/ragflow-logs' -czf /tmp/xd_ragflow-code-20260329.tar.gz .
```
把这两个文件拷到离线服务器
```bash
/tmp/ragflow-offline-images-20260329.tar
/tmp/xd_ragflow-code-20260329.tar.gz
```
在离线服务器上解压代码
```bash
mkdir -p /opt/xd_ragflow
tar -xzf /tmp/xd_ragflow-code-20260329.tar.gz -C /opt/xd_ragflow
```
在离线服务器上导入镜像
```bash
docker load -i /tmp/ragflow-offline-images-20260329.tar
```
在离线服务器上准备运行目录
```bash
cd /opt/xd_ragflow
mkdir -p history_data_agent docker/ragflow-logs
```
在离线服务器上启动
```bash
cd /opt/xd_ragflow
docker compose -f docker/docker-compose.yml -f docker/docker-compose.offline.yml up -d
```
查看状态
```bash
cd /opt/xd_ragflow
docker compose -f docker/docker-compose.yml -f docker/docker-compose.offline.yml ps
docker compose -f docker/docker-compose.yml -f docker/docker-compose.offline.yml logs -f ragflow-cpu
```
访问服务`http://离线服务器IP:80`

补充说明

- 这次是不带数据迁移，所以离线机上的 MySQL、MinIO、ES、Redis 都会是全新初始化。
- 如果离线服务器不是 `linux/amd64`，第 1 步里的 `--platform linux/amd64` 要改成目标架构。
- 如果你后面想换镜像标签，只要同时改 `docker/.env` (line 108) 和 `docker build` 的 `-t` 即可。

- 我这次已经校验过 `docker compose config`，确认它现在会引用 `ragflow-local:20260329`，并且保留
`USE_DOCLING=false、USE_MINERU=false` 的离线安全设置；但我还没有实际替你执行完整 `docker build` 和 `up`。如果你愿意，我下一步可以继续帮你把这份命令清单整理成一份可直接执行的 `deploy_offline.sh`。
